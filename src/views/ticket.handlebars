<div>
    <h1>Ticket Nº {{_id}}</h1>
    <h2>Comprador {{purchaser}}</h2>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Producto</th>
                <th scope="col">Imagen</th>
                <th scope="col">Cantidad</th>
                <th scope="col">Precio</th>
                <th scope="col"><a href="http://localhost:8080/products">Homepage</a></th>
            </tr>
        </thead>
        <tbody>
            {{#each products}}
                <tr>
                    <td>
                        {{@index}}
                    </td>
                    <td>
                        {{this.pid.title}}
                    </td>
                    <td>
                        <img src={{this.pid.thumbnail}} alt={{this.pid.title}} height="50px" width="auto" />
                    </td>
                    <td>{{this.quant}}</td>
                    <td>${{this.pid.price}}</td>

                </tr>
              {{/each}}
            <tr>
                <th scope="row"></th>
                <td></td>
                <td></td>
                <td></td>
                <td>Total: $ {{amount}}</td>
                <td></td>
            </tr>
        </tbody>
    </table>
</div>


<script src="/js/cart.js"></script>